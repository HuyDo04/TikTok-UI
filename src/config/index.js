const config = {
    routes : {
        home : "/",
        //product
        products : "/products",
        //auth
        login : "/login",
        register : "/register",
        user: "/user",
        profile:"/profile",
        notFound:"*"
    }
}

export default config;